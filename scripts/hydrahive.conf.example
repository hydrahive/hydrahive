# hydrahive.conf — Lokale Deploy-Konfiguration
# Kopieren nach hydrahive.conf und anpassen (wird nicht eingecheckt)

VM="hydrahive@<YOUR_VM_IP>"       # IP/Hostname der HydraHive VM
SSH_KEY="$HOME/.ssh/id_rsa"       # SSH-Key für Verbindung zur VM

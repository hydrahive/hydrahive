id: proxmox-manager
name: Proxmox Manager
version: 1.0.0
description: Proxmox VE verwalten — Nodes, VMs überwachen und VM-Aktionen ausführen.
author: HydraHive
type: tool
runtime: python
permissions: []
auto_attach: false
config_fields:
  - key: base_url
    label: Base URL
    type: string
  - key: api_key
    label: API Token (user@realm!tokenid=secret)
    type: password

id: sonarr-manager
name: Sonarr Manager
version: 1.0.0
description: Sonarr v3 API — Serien verwalten, suchen, hinzufügen und Queue überwachen.
author: HydraHive
type: tool
runtime: python
permissions: []
auto_attach: false
config_fields:
  - key: base_url
    label: "Base URL (z.B. http://localhost:8989)"
    type: string
  - key: api_key
    label: "API Key"
    type: password

id: radarr-manager
name: Radarr Manager
version: 1.0.0
description: Radarr v3 API — Filme verwalten, suchen, hinzufügen und Queue überwachen.
author: HydraHive
type: tool
runtime: python
permissions: []
auto_attach: false
config_fields:
  - key: base_url
    label: "Base URL (z.B. http://localhost:7878)"
    type: string
  - key: api_key
    label: "API Key"
    type: password
